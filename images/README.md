# Images

This directory is reserved for image assets used by the Synch Pipe Arcade Hub.

All pixel art and sprites are rendered via CSS or Canvas API — no binary image files are required.
Place any custom `.png`, `.jpg`, or `.gif` assets here as needed.
